# - Commands -

alias off='shutdown now'
alias blackcategories="pacman -Sg | grep blackarch | sed 's/blackarch-/ /'"
#alias rmss='rm ~/Screenshots/*'


# - Scripts -
alias lsa='lsd -a'
alias ll='lsd -l'
alias cat='bat'
alias grep='rg'


# - Shortcuts -

alias v='nvim'
alias vim='nvim'
alias c='wl-copy'
alias p='python3'
alias ls='lsd'
alias bksv='ssh m0r4a_low@192.168.1.150 -p 22'
alias mirror_refresh='sudo reflector --verbose --latest 15 --sort rate --save /etc/pacman.d/mirrorlist'

# - Docker -
alias dc='docker-compose'
alias doc-ip='docker inspect -f "{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}"'

# - Electron apps 
alias vesktop='vesktop --enable-features=UseOzonePlatform --ozone-platform=wayland'
alias spotify='spotify --enable-features=UseOzonePlatform --ozone-platform=wayland'

# - Rust -

#alias rsc='nvim src/main.rs'

## -- Cargo --

#alias cn='cargo new'
#alias cb='cargo build'
#alias cr='cargo run'

# - Git -

alias g='git'
alias ga='git add'
alias gs='git status'
alias gc='git clone'

# - Edit files -

alias md='bash ~/scripts/apostrophe'

# - Quick directories -

## -- GitHub repos --
alias cs='cd ~/workspace/CS50P/'

## -- go --

alias godsk='cd ~/Desktop'
alias goh='cd ~'
alias godw='cd ~/Downloads'
alias gosc='cd ~/scripts'
#alias sdev='cd ~/workspace/scriptDev'

# - Configs -

alias cfgbash='nvim ~/.bashrc'
alias cfghypr='nvim ~/.config/hypr/hyprland.conf'
alias cfgzsh='nvim ~/.zshrc'
alias cfgkitty='nvim ~/.config/kitty/kitty.conf'
alias cfgstarship='nvim ~/.config/starship/starship.toml'
